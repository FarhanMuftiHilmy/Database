CREATE TABLE IF NOT EXISTS contoh_table_serius(
id INT AUTO_INCREMENT PRIMARY KEY,
judul VARCHAR<255> NOT NULL
mulai DATE,
selesai DATE,
status VARCHAR(15) NOT NULL,
deskripsi TEXT,
dibuat_pada TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
